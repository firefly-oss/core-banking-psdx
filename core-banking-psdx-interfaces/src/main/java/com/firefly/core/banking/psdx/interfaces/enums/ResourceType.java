package com.firefly.core.banking.psdx.interfaces.enums;

/**
 * Enum representing the type of resource being accessed.
 */
public enum ResourceType {
    ACCOUNT,
    TRANSACTION,
    BALANCE,
    PAYMENT,
    CARD,
    CARD_TRANSACTION,
    CARD_BALANCE,
    FUNDS_CONFIRMATION,
    CONSENT
}
